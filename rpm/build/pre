if [ "$1" = install ]; then
  if [ -z `grep cloudwatchd /etc/shadow` ]
    then adduser --system --shell /sbin/nologin cloudwatchd --gid 0 --ne-create-home
  fi
fi

ln -s /usr/local/sbin/start-stop-daemon."$(uname -p)" /usr/sbin/start-stop-daemon
